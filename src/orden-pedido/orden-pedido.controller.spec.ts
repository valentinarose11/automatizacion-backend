import { Test, TestingModule } from '@nestjs/testing';
import { OrdenPedidoController } from './orden-pedido.controller';

describe('OrdenPedido Controller', () => {
  let controller: OrdenPedidoController;

  beforeEach(async () => {
    // const module: TestingModule = await Test.createTestingModule({
    //   controllers: [OrdenPedidoController],
    // }).compile();

    // controller = module.get<OrdenPedidoController>(OrdenPedidoController);
  });

  it('should be defined', () => {
    // expect(controller).toBeDefined();
  });
});
